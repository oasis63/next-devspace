// // useLayoutEvents.ts
// import { useState } from "react";

// const useLayoutEvents = () => {
//   const [layoutEvent, setLayoutEvent] = useState<any | null>(null);

//   const handleLayoutEvent = (eventData: any) => {
//     setLayoutEvent(eventData);
//   };

//   return { layoutEvent, handleLayoutEvent };
// };

// export default useLayoutEvents;
